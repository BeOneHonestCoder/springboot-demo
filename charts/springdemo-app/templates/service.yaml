apiVersion: v1
kind: Service
metadata:
  name: {{ include "springdemo-app.fullname" . }}
  labels:
    {{- include "springdemo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "springdemo-app.selectorLabels" . | nindent 4 }}
