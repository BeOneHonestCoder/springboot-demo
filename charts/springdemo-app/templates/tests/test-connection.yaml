apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "springdemo-app.fullname" . }}-test-connection"
  labels:
    {{- include "springdemo-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "springdemo-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
